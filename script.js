let bgColor = prompt("Enter the bgcolor");
if (bgColor) {
    document.body.style.backgroundColor = bgColor;
}
let text = prompt("Enter the text color");
if(text){
    document.body.style.color = text;
}